<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/timetable_114px"
        android:label="Training Course"
        android:roundIcon="@mipmap/timetable_114px"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.project.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="Add now course"
            android:name="com.example.project.AddCourseActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>

    </application>

</manifest>